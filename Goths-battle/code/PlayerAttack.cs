using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAttack : MonoBehaviour
{

	//tout va se passer dans update normalement vu que je veux que le script vérifie à tout moment que je n'appuie pas sur e et une flèche directionnel
	//public Animator attacks;
	// Start is called before the first frame update
	void Start()
	{
		
	}

	// Update is called once per frame
	void Update()
	{
		//Att.SetActive(false);
		//if(Input.GetKeyDown(KeyCode.E))
		{
			//attacks.SetBool("Att",true);
			
		}
	}
}
